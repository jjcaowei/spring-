针对并发不停扩大的一些改进措施：
    在系统层面增加机器集群，机器集群负责分解用户的请求，从而使机器的负载降低；
    分库分表，读写分离；主从表，主表负责数据库的写操作，从表负责数据库的读操作；
    如果落到数据库的请求还是不停地在增大，此时我们可以根据系统的特性，针对读多写少的请求，我们引入缓存集群。具体地讲，就是在写数据库的时候
    同时写一部分数据到缓存集群，缓存集群可以来承载大部分的读请求；
    针对大量地写操作，解决的措施可以采用中间件技术，中间件能够很好地做到写请求异步处理。
